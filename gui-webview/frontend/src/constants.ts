export enum View {
  Installed,
  FilterInstalled,
  ReconcileInstalled,
  Search,
  Reconcile,
}

export enum ListFormat {
  Dense,
  Compact,
  Expanded,
}

export enum AddonAction {
  VisitHomepage,
  ViewChangelog,
  RevealFolder,
  Resolve,
  Rollback,
  Pin,
  Unpin,
  Unreconcile,
  InstallAndReplace,
}
